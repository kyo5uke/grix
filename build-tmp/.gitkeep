# Keeps build-tmp/ present in checkouts: .cargo/config.toml redirects the
# toolchain's TMP/TEMP here, and MSVC's link.exe fails with LNK1104 if the
# directory does not exist (e.g. on a fresh CI runner).
